# Helper for consistent documentation of `x` where input can be codelist or cohort.

Helper for consistent documentation of `x` where input can be codelist
or cohort.

## Arguments

- x:

  A codelist or cohort table name.
