# Empty code search object

Empty code search object

## Usage

``` r
emptyCodeSearch(searchStrategy = NULL)
```

## Arguments

- searchStrategy:

  The search strategy used to generate the candidate codes.

## Value

An empty `code_search` object.
